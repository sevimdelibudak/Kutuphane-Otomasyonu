@model KutuphaneOtomasyonu.Models.Uye

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Uye</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="KimlikNo" class="control-label"></label>
                <input asp-for="KimlikNo" class="form-control" value="@Model.KimlikNo" />
                <span asp-validation-for="KimlikNo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AdSoyad" class="control-label"></label>
                <input asp-for="AdSoyad" class="form-control"  value="@Model.AdSoyad" />
                <span asp-validation-for="AdSoyad" class="text-danger"></span>
            </div>
            <div class="form-group">
                @*<label asp-for="TurID" class="control-label"></label>
        <input asp-for="TurID" class="form-control"  value="@Model.TurID" />
        <span asp-validation-for="TurID" class="text-danger"></span>*@
                @Html.DropDownListFor(model=>model.TurID, (List<SelectListItem>)ViewBag.UyeTurleri, "Üye Türü Seçiniz", new { @class = "form-control mt-2", })
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="UyeListele">Listeye Dön</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
